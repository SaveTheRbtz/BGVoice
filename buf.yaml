version: v2

modules:
  - path: proto

deps:
  - buf.build/googleapis/googleapis

lint:
  use:
    - STANDARD
  except:
    # AIP-131 Get methods return the resource itself, not a wrapper response.
    - RPC_RESPONSE_STANDARD_NAME

breaking:
  use:
    - FILE
